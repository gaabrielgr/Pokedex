import styled from 'styled-components';

export const NotFoundStyles = styled.img`
    src: ${props => props.src};
    width: 50%;
    display: flex;
    justify-content: center;
    margin: 250px auto;

`